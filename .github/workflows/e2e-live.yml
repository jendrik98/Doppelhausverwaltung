name: Live E2E – Mietverwaltung

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'src/**'
      - 'scripts/**'
      - 'index.html'
      - 'style.css'
      - 'app.js'
      - 'service-worker.js'
      - 'manifest.webmanifest'
      - 'legal-rules.json'
      - 'tests/**'
      - 'playwright.config.js'
      - 'package.json'
      - 'package-lock.json'
      - 'tsconfig.json'
      - '.github/workflows/ci.yml'
      - '.github/workflows/e2e-live.yml'

permissions:
  contents: read

concurrency:
  group: live-e2e-${{ github.ref }}
  cancel-in-progress: true

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      E2E_BASE_URL: https://jendrik98.github.io/Doppelhausverwaltung/
    steps:
      - name: Repository auschecken
        uses: actions/checkout@v4
      - name: Node einrichten
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - name: E2E-Abhängigkeiten installieren
        run: npm ci
      - name: Chromium installieren
        run: npx playwright install --with-deps chromium
      - name: PDF-Prüfwerkzeuge installieren
        run: |
          sudo apt-get update
          sudo apt-get install -y poppler-utils
          pdfinfo -v
          pdftotext -v
      - name: Auf exaktes Live-Deployment warten
        shell: bash
        run: |
          set -euo pipefail
          EXPECTED_APP_SHA="$(sha256sum app.js | awk '{print $1}')"
          EXPECTED_STYLE_SHA="$(sha256sum style.css | awk '{print $1}')"
          echo "Erwarte app.js ${EXPECTED_APP_SHA} und style.css ${EXPECTED_STYLE_SHA} von Commit ${GITHUB_SHA}."
          for i in {1..60}; do
            APP_TMP="$(mktemp)"
            STYLE_TMP="$(mktemp)"
            if curl -fsSL --max-time 15 "${E2E_BASE_URL}app.js?deploy=${GITHUB_SHA}-${i}" -o "$APP_TMP" \
              && curl -fsSL --max-time 15 "${E2E_BASE_URL}style.css?deploy=${GITHUB_SHA}-${i}" -o "$STYLE_TMP"; then
              LIVE_APP_SHA="$(sha256sum "$APP_TMP" | awk '{print $1}')"
              LIVE_STYLE_SHA="$(sha256sum "$STYLE_TMP" | awk '{print $1}')"
              rm -f "$APP_TMP" "$STYLE_TMP"
              if [ "$LIVE_APP_SHA" = "$EXPECTED_APP_SHA" ] && [ "$LIVE_STYLE_SHA" = "$EXPECTED_STYLE_SHA" ]; then
                echo "Exaktes Live-Deployment ist verfügbar."
                exit 0
              fi
              echo "Noch nicht aktuell ($i/60): app=${LIVE_APP_SHA}/${EXPECTED_APP_SHA}, style=${LIVE_STYLE_SHA}/${EXPECTED_STYLE_SHA}"
            else
              rm -f "$APP_TMP" "$STYLE_TMP"
              echo "Live-Dateien noch nicht abrufbar ($i/60)."
            fi
            sleep 10
          done
          echo "Die exakten app.js/style.css-Dateien dieses Commits wurden innerhalb von 10 Minuten nicht gefunden."
          exit 1
      - name: Preflight-Verträge prüfen
        run: |
          npm run typecheck
          npm run test:validation
          npm run test:architecture
          npm run test:quality
          npm run test:pwa
      - name: Echter Browser-E2E-Test
        run: npm run test:e2e
      - name: Playwright-Bericht sichern
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: |
            playwright-report/
            test-results/
          retention-days: 14
